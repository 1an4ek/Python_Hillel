def my_range(start, stop, step=1):
    rangelist = [start]
    while (start + step) < stop:
        start += step
        rangelist.append(start)
    return rangelist


print(my_range(int(input()), int(input()), int(input())))
